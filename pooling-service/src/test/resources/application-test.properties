# Test Configuration for Pooling Service
spring.application.name=pooling-service-test
server.port=0

# Use the same MongoDB Atlas connection as production for integration tests
# Comment out these lines if you want to skip MongoDB tests entirely
spring.data.mongodb.uri=mongodb+srv://2022cs056:dH4aTFn3IOerWlVZ@cluster0.9ccambx.mongodb.net/islandhop_pooling_test?retryWrites=true&w=majority
spring.data.mongodb.database=islandhop_pooling_test

# Disable Kafka for tests
spring.kafka.bootstrap-servers=
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# Disable external service calls for tests
services.trip-planning.url=http://localhost:8084
services.user-services.url=http://localhost:8080

# Test logging
logging.level.com.islandhop.pooling=DEBUG
logging.level.org.springframework.test=INFO
logging.level.org.junit=INFO

# Disable actuator security for tests
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
