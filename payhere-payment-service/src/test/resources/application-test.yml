spring:
  profiles:
    active: test
  data:
    mongodb:
      uri: mongodb://localhost:27017/test_islandhop_trips
      database: test_islandhop_trips

# PayHere Configuration for testing
payhere:
  merchant-id: test-merchant-id
  secret: test-secret
  notify-url: http://localhost:8088/api/v1/payments/notify
  sandbox: true

# Disable logging during tests
logging:
  level:
    root: WARN
    com.islandhop.payment: INFO
