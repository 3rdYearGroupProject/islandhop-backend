spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://2022cs056:dH4aTFn3IOerWlVZ@cluster0.9ccambx.mongodb.net/islandhop_trips?retryWrites=true&w=majority}
      database: islandhop_trips
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?user=postgres.hgpicovzphnrhsdhggqs&password=islandhop123#&prepareThreshold=0&preparedStatementCacheQueries=0
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 300000
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 10000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
      auto-commit: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
        connection:
          provider_disables_autocommit: true

google:
  maps:
    api-key: AIzaSyCCk35BJWjRvG7KHskc0GETOWe0qrBE-Go

logging:
  level:
    root: INFO
    com.islandhop.tripinit: DEBUG

server:
  port: 8095
  servlet:
    context-path: /api

cors:
  allowed-origins: http://localhost:3000
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true
