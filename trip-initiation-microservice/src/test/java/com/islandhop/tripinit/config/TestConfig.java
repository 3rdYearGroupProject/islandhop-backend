package com.islandhop.tripinit.config;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * Test configuration class that provides mock beans for testing.
 * This class provides test-specific configuration.
 */
@TestConfiguration
public class TestConfig {
    // No additional beans needed since production config provides all required beans
}
